[project]
name = "hashid-scripts"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "cryptography>=42.0.0",
    "unsloth",
    "trl>=0.12.0,<0.15.0",
    "datasets",
    "transformers",
    "torch",
]
